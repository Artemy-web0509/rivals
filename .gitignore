# runtime binaries (not source)
node
cloudflared
node_modules/

# tunnels / local infra
.tunnel_pid
.tunnel_url
tunnel.pid
tunnel.log
server.log

# OS / editor junk
.DS_Store

# nested separate repo (deployed build) — not part of this project
deploy/
